#pragma once

// Defines the default frames per second (FPS)
#define DEFAULT_FPS           60 // 60 FPS provides a good balance between performance and rendering quality

// Enable or disable the cursor by default
#define DEFAULT_CURSOR_ENABLE 1 // 1 enables the cursor, 0 disables it